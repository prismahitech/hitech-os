# PRISMA Canonical Promotion Readiness

Phase: `CANONICAL_PROMOTION_READINESS_RESOLUTION`

This root is a new evidence layer over the immutable certified corpus. It does not replace or rewrite `governance/visual-promotion/contracts/corpus-certification/`.

## Goal

Resolve as much canonical meaning, Identity recipe/adapter, exact target/binding and projection authority as existing evidence can prove, while keeping unknowns explicit.

The phase ends before global canonical authority or product/runtime mutation.

## Lane roots

- `tablet/` — Chat 1
- `pc/` — Chat 2
- `mobile/` — Chat 3
- `shared-ui/` — Chat 4
- `atlasfin/` — Chat 5 read-only reference analysis
- `contracts/` and `global/` — Chat 6

See `contracts/promotion-readiness.contract.json` and the canonical parallel Chat prompt for exact outputs, statuses and gates.

## Invariants

- 2,097 certified targets in, exactly 2,097 target resolutions out.
- No recensus or broad rediscovery.
- No canonical ID minting in worker lanes.
- Atlasfin supports evidence but never becomes semantic authority.
- Materiality remains `STANDBY_USER_INVOKED_ONLY`.
- Recipe similarity alone is visual similarity, not semantic equivalence.
- No product/runtime/CSS/TSX/projection mutation.
- No global canonical registration before the final plan and a separately authorized integration subphase.
