import { PageLoading } from "@components/ui/page-loading";

export default function InboxLoading() {
  return <PageLoading title="Loading inbox" subtitle="Hydrating records, filters and list chrome." variant="list" />;
}
