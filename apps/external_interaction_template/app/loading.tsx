import { PageLoading } from "@components/ui/page-loading";

export default function RootLoading() {
  return <PageLoading title="Loading workspace" subtitle="Bootstrapping shell surfaces, summaries and route chrome." variant="dashboard" />;
}
