TASK_ID: DF-20260401-1518-flatten-hardening-r1  # INFERRED
PROJECT: deltaforge  # INFERRED
MODE: reference  # INFERRED
BASE_COMMIT: d4e84d441cbd236fe258aa09f4c5b63b5283c405  # INFERRED
SOURCE_OF_TRUTH:
- apps/deltaforge source tree (application/bootstrap/domain/infrastructure/ui)  # USER_EXPLICIT
- apps/deltaforge/pyproject.toml packaging contract  # USER_EXPLICIT
- apps/deltaforge/docs/maestro_handoff/*.md documentation contract  # USER_EXPLICIT
- user prompt constraints for flatten hardening and verification depth  # USER_EXPLICIT
IN_SCOPE:
- harden flattened import/runtime strategy without reintroducing nested package  # USER_EXPLICIT
- validate runtime/packaging from non-local cwd contexts  # USER_EXPLICIT
- fix pyproject package discovery/entrypoint consistency  # USER_EXPLICIT
- align smoke tests and add high-value guards for flatten contract  # USER_EXPLICIT
- fully align docs/maestro_handoff references with flattened layout  # USER_EXPLICIT
OUT_OF_SCOPE:
- broad UI redesign or feature expansion unrelated to flatten hardening  # USER_EXPLICIT
- reintroducing apps/deltaforge/deltaforge nested package unless correctness requires it  # USER_EXPLICIT
- modifying historical .bak handoff snapshots  # INFERRED
ACCEPTANCE_CRITERIA:
- pytest passes from apps/deltaforge and from repo root invocation path  # USER_EXPLICIT
- editable install succeeds and deltaforge entrypoint resolves from external cwd  # USER_EXPLICIT
- no active source code import paths rely on old deltaforge.* prefix  # USER_EXPLICIT
- pyproject declares explicit package discovery compatible with flat layout  # USER_EXPLICIT
- docs/maestro_handoff no longer describe nested apps/deltaforge/deltaforge layout  # USER_EXPLICIT
VALIDATION_COMMANDS:
- python -m pytest  # USER_EXPLICIT
- python -m pytest apps/deltaforge/tests -q  # USER_EXPLICIT
- python -m pip install -e F:\repos\hitech-os\apps\deltaforge  # USER_EXPLICIT
- external cwd probe: python -c "import application, bootstrap, domain, infrastructure, ui, deltaforge_app"  # INFERRED
- external cwd probe: python -c "import importlib.metadata as m; [e for e in m.entry_points(group='console_scripts') if e.name=='deltaforge'][0]"  # INFERRED

EXECUTION_STATUS: completed  # INFERRED
VALIDATION_RESULT: pytest_local=pass; pytest_repo_root=pass; editable_install=pass; external_cwd_import_probe=pass  # INFERRED