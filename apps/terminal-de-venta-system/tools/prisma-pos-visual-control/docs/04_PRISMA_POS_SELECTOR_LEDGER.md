# Selector Ledger

## POS

`.posWorkspace`, `.catalogArea`, `.searchCard`, `.searchInputWrap`, `.categoryRail`, `.categoryButton`, `.categoryButtonActive`, `.productGrid`, `.productCard`, `.productCard::before`, `.productImageStage`, `.productAura`, `.productPedestal`, `.productPackshot`, `.productText`, `.productAside`, `.addButton`, `.ticketPanel`, `.ticketHeader`, `.ticketLines`, `.ticketLine`, `.cartThumb`, `.stepper`, `.lineTotal`, `.ticketTotal`, `.checkoutLink`, `.checkoutLinkDisabled`, `.secondaryCheckoutActions`, `.statePanel`, `.emptyTicket`, `.errorBanner`, `.successCard`.

## Shell

`.shell`, `.shell::before`, `.sidebar`, `.header`, `.content`.

Regla: pseudo-elementos y z-index se registran antes de agregar nuevos brillos.
