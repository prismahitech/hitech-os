# QA Gates

- Masterplan: instala docs/diagrams/inventory y rollback funciona.
- Control Plane: tuner lista tokens, presets y genera CSS.
- Surface Lock: packshots sin checkerboard, ticket premium, `COBRAR` dominante, sin romper agregar/quitar.
- Shell Atmosphere: fondo no compite con producto, sidebar no compite con carrito, reduced motion respetado.

Capturas esperadas por fase: /pos completo, crop cards, crop carrito, crop sidebar/header, Tablet, PC como no-regression.
