# products/0.backgrounds

Biblioteca central de backgrounds para todas las apps del repo.

Las apps no cargan `F:\...` en runtime; reciben un mirror en `app/public/visual-backgrounds/<app>/`.

El result ZIP de bgcore fix2 guarda manifests y hashes, no imagenes pesadas.
