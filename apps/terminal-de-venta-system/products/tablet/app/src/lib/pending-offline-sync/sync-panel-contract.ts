export type PendingSendStatus="pending"|"failed"|"sent"|"acked"|"conflict";export type SyncRisk="ok"|"warn"|"danger";export type SyncPanelItem={id:string;eventId:string;title:string;description:string;status:PendingSendStatus;statusLabel:string;risk:SyncRisk;attempts:number;createdAt:string;canRetry:boolean};export type SyncPanelSummary={total:number;pending:number;failed:number;sent:number;acked:number;conflict:number;risk:SyncRisk;headline:string;operatorMessage:string;offlineVisible:boolean;lastCheckedAt:string};export type SyncPanelResponse={summary:SyncPanelSummary;items:SyncPanelItem[];diagnostics:string[]};export const SAFE_SYNC_COPY={ready:"Todo enviado",pending:"Hay información guardada localmente esperando envío.",failed:"Hay eventos fallidos que necesitan reintento.",conflict:"Hay información que requiere revisión antes de enviarse.",offline:"Sin conexión visible. La Tablet sigue trabajando en local."} as const;
