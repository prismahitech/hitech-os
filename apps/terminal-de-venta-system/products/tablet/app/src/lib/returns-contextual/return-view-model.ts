import type{SalesTodayTicket}from"@/lib/sales-today/types";import type{ReturnSelection}from"./types";import{returnReasonLabel}from"./return-reasons";export function selectedReturnLines(t:SalesTodayTicket,s:ReturnSelection){return t.lines.map(l=>({...l,qty:Math.min(s[l.id]??0,l.qty)})).filter(l=>l.qty>0)}export function returnAmountCents(t:SalesTodayTicket,s:ReturnSelection){return selectedReturnLines(t,s).reduce((sum,l)=>sum+l.qty*l.priceCents,0)}export function buildReturnPayload(t:SalesTodayTicket,s:ReturnSelection,reason:string,notes:string){return{businessId:"biz_tablet_standalone",saleId:t.saleId,saleFolio:t.folio,reason,reasonLabel:returnReasonLabel(reason),notes,amountCents:returnAmountCents(t,s),cashier:t.cashier,lines:selectedReturnLines(t,s).map(l=>({productId:l.productId,sku:l.sku,name:l.productName,qty:l.qty,unitPriceCents:l.priceCents}))}}
