import { formatMoney } from "@/lib/pos/cart-state";
import type { ShiftCashSummary, ShiftKpi } from "./shift-cash-closure-contract";
import { SHIFT_VISIBLE_COPY } from "./shift-cash-closure-contract";
export function cashInputToCents(value:string){const n=Number(value.replace(/,/g,"").trim());return Number.isFinite(n)&&n>=0?Math.round(n*100):0;}
export function varianceTone(value:number|null|undefined){const abs=Math.abs(value??0);if(abs===0)return "ok";if(abs<=5000)return "warn";return "danger";}
export function buildClosePreview(shift:ShiftCashSummary|null,countedCashInput:string){if(!shift||shift.status!=="OPEN")return{varianceCents:0,copy:"Abre turno para calcular diferencia."};if(!countedCashInput.trim())return{varianceCents:0,copy:"Captura el conteo fisico para ver diferencia."};const varianceCents=cashInputToCents(countedCashInput)-shift.expectedCashCents;if(varianceCents===0)return{varianceCents,copy:"Caja cuadrada. Milagro contable, pero se acepta."};return varianceCents>0?{varianceCents,copy:"Sobra efectivo contra lo esperado."}:{varianceCents,copy:"Falta efectivo contra lo esperado."};}
export function shiftStatusCopy(shift:ShiftCashSummary|null,state:string){if(state==="loading")return{badge:"Revisando caja",title:"Consultando turno",detail:"Validando estado local de caja."};if(!shift)return{badge:SHIFT_VISIBLE_COPY.noShiftBadge,title:SHIFT_VISIBLE_COPY.noShiftTitle,detail:"La venta queda bloqueada hasta registrar caja inicial y cajero. Sin turno, el dinero queda como chisme de pasillo."};if(shift.status==="OPEN")return{badge:SHIFT_VISIBLE_COPY.openShiftBadge,title:SHIFT_VISIBLE_COPY.openShiftTitle,detail:shift.operatorMessage};return{badge:SHIFT_VISIBLE_COPY.closedShiftBadge,title:SHIFT_VISIBLE_COPY.closedShiftTitle,detail:shift.operatorMessage};}
export function buildShiftKpis(shift:ShiftCashSummary|null):ShiftKpi[]{if(!shift)return[{label:"Estado",value:"Bloqueado",hint:"Abre turno para vender."},{label:"Caja inicial",value:formatMoney(0),hint:"Pendiente de captura."},{label:"Ventas",value:"0",hint:"Sin turno abierto."},{label:"Esperado",value:formatMoney(0),hint:"Sin calculo todavia."}];return[{label:"Estado",value:shift.status==="OPEN"?"Abierto":"Cerrado",hint:shift.canSell?"Venta permitida.":"Venta bloqueada."},{label:"Caja inicial",value:formatMoney(shift.cashStartCents),hint:`Cajero: ${shift.cashier}`},{label:"Ventas",value:String(shift.salesCount),hint:formatMoney(shift.salesTotalCents)},{label:"Esperado",value:formatMoney(shift.expectedCashCents),hint:shift.varianceCents===null?"Pendiente de conteo.":`Diferencia: ${formatMoney(shift.varianceCents)}`}];}
