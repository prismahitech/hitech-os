import{prisma}from"../prisma/client";import{getTabletRuntimeMeta}from"../pos-runtime";import{riskForStatus,statusLabel,summarizeSync,topicTitle}from"@/lib/pending-offline-sync/sync-panel-view-model";import type{PendingSendStatus,SyncPanelItem}from"@/lib/pending-offline-sync/sync-panel-contract";type RawEvent={id:string;businessId:string;topic:string;aggregateId:string;payloadJson:string;status:string;attempts:number;createdAt:Date;sentAt:Date|null;lastError:string|null};function safeDescription(r:RawEvent){const b=r.lastError?"Necesita revisión antes de volver a enviar.":"Guardado localmente para continuidad de operación.";if(r.topic.includes("sale"))return`Venta registrada en la Tablet. ${b}`;if(r.topic.includes("stock")||r.topic.includes("inventory"))return`Movimiento de inventario guardado. ${b}`;if(r.topic.includes("shift"))return`Movimiento de turno guardado. ${b}`;return`Evento operativo guardado. ${b}`}function toItem(r:RawEvent):SyncPanelItem{const status=r.status.toLowerCase() as PendingSendStatus;let eventId=r.id;try{const p=JSON.parse(r.payloadJson);if(typeof p.eventId==="string")eventId=p.eventId}catch{}return{id:r.id,eventId,title:topicTitle(r.topic),description:safeDescription(r),status,statusLabel:statusLabel(status),risk:riskForStatus(status,r.attempts),attempts:r.attempts,createdAt:r.createdAt.toISOString(),canRetry:status==="pending"||status==="failed"||status==="conflict"}}export async function buildPendingOfflineSyncPanel(input:{businessId:string;limit:number;status?:string|null}){const where:any={businessId:input.businessId};if(input.status)where.status={in:[input.status,input.status.toUpperCase(),input.status.toLowerCase()]};const rows=await prisma.outboxEvent.findMany({where,orderBy:{createdAt:"desc"},take:input.limit});const items=rows.map((r:any)=>toItem(r));const summary=summarizeSync(items);const runtime=getTabletRuntimeMeta();return{summary,items,diagnostics:[runtime.localSalesAllowed?"Venta local disponible":"Revisar venta local",summary.offlineVisible?"Hay trabajo local por enviar":"No hay pendientes visibles"]}}export async function requestPendingRetry(input:{businessId:string;ids?:string[];includeFailed?:boolean;includePending?:boolean}){const states=["conflict"];if(input.includeFailed!==false)states.push("failed","FAILED");if(input.includePending)states.push("pending","PENDING");const where:any={businessId:input.businessId,status:{in:states}};if(input.ids?.length)where.id={in:input.ids};const result=await prisma.outboxEvent.updateMany({where,data:{status:"pending",attempts:{increment:1},lastError:null}});return{updated:result.count,message:result.count>0?"Reintento preparado. Se enviará cuando haya conexión.":"No había eventos listos para reintentar."}}
