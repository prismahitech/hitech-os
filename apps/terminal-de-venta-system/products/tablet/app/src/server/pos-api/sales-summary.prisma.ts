import{prisma}from"../prisma/client";import type{SalesTodayInput}from"./validators";function localDayRange(dateText?:string){const base=dateText?new Date(`${dateText}T00:00:00`):new Date();if(Number.isNaN(base.getTime()))throw new Error("INVALID_DATE");const from=new Date(base.getFullYear(),base.getMonth(),base.getDate());const to=new Date(base.getFullYear(),base.getMonth(),base.getDate()+1);return{from,to,date:`${from.getFullYear()}-${String(from.getMonth()+1).padStart(2,"0")}-${String(from.getDate()).padStart(2,"0")}`}}export async function getTodaySalesSummary(input:SalesTodayInput){const{from,to,date}=localDayRange(input.date);const sales=await prisma.sale.findMany({where:{businessId:input.businessId,status:"COMPLETED",createdAt:{gte:from,lt:to},...(input.terminalId?{terminalId:input.terminalId}:{})},include:{lines:true},orderBy:{createdAt:"desc"}});const totalCents=sales.reduce((s:any,x:any)=>s+x.totalCents,0);const unitsSold=sales.flatMap((s:any)=>s.lines).reduce((s:any,l:any)=>s+l.qty,0);const topProducts:any[]=[];return{businessId:input.businessId,terminalId:input.terminalId??null,date,salesCount:sales.length,ticketsClosed:sales.length,totalCents,total:totalCents/100,averageTicketCents:sales.length?Math.round(totalCents/sales.length):0,averageTicket:sales.length?Math.round(totalCents/sales.length)/100:0,unitsSold,topProducts,tickets:sales.map((sale:any)=>({saleId:sale.id,folio:sale.folio,terminalId:sale.terminalId,cashier:sale.cashier,status:sale.status,createdAt:sale.createdAt.toISOString(),totalCents:sale.totalCents,lineCount:sale.lines.length,unitsSold:sale.lines.reduce((s:any,l:any)=>s+l.qty,0),lines:sale.lines.map((l:any)=>({id:l.id,productId:l.productId,sku:l.sku,productName:l.productName,qty:l.qty,priceCents:l.priceCents,totalCents:l.totalCents}))}))}}
