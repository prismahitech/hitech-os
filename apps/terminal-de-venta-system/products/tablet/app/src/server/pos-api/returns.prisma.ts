import{prisma}from"../prisma/client";import{makePosId}from"../pos-engine/ids";export async function createSaleReturn(input:any){const now=new Date();const saleReturn=await prisma.saleReturn.create({data:{id:makePosId("return"),businessId:input.businessId,saleFolio:input.saleFolio,reason:input.reasonLabel||input.reason,amountCents:input.amountCents,status:"CREATED",cashier:input.cashier,createdAt:now}});await prisma.outboxEvent.create({data:{id:makePosId("event"),businessId:input.businessId,topic:"sale.return.created",aggregateId:saleReturn.id,status:"pending",createdAt:now,payloadJson:JSON.stringify({eventId:saleReturn.id,topic:"sale.return.created",businessId:input.businessId,actorId:input.cashier,source:"tablet-pos-contextual-return",occurredAt:now.toISOString(),schemaVersion:"1.0",payload:{saleFolio:input.saleFolio,amountCents:input.amountCents,lines:input.lines}})}});return{returnId:saleReturn.id,saleFolio:saleReturn.saleFolio,reason:saleReturn.reason,amountCents:saleReturn.amountCents,status:saleReturn.status,cashier:saleReturn.cashier,createdAt:saleReturn.createdAt.toISOString()}}
