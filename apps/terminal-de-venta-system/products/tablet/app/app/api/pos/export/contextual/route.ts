import{toPosApiError}from"@/server/pos-api/errors";import{ok}from"@/server/pos-api/responses";import{buildContextualExportReport}from"@/server/pos-export/contextual";export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(request:Request){try{const p=new URL(request.url).searchParams;const surface=p.get("surface")??"home";const businessId=p.get("businessId")??"biz_demo_001";const report=await buildContextualExportReport({businessId,surface});return ok(report,undefined,{endpoint:"GET /api/pos/export/contextual",businessId,surface,message:"Reporte contextual listo"})}catch(error){return toPosApiError(error)}}
