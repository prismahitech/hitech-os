import{toPosApiError}from"@/server/pos-api/errors";import{ok}from"@/server/pos-api/responses";import{readPosListInput}from"@/server/pos-api/validators";import{buildPendingOfflineSyncPanel}from"@/server/pos-sync-panel";export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(request:Request){try{const input=readPosListInput(new URL(request.url).searchParams,80,200);const panel=await buildPendingOfflineSyncPanel(input);return ok(panel,undefined,{endpoint:"GET /api/pos/sync/panel",businessId:input.businessId})}catch(error){return toPosApiError(error)}}
