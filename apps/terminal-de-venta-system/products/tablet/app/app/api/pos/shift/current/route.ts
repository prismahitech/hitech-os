import { fail, ok } from "@/server/pos-api/responses";import { readCurrentShiftInput, shiftCashRepository, ShiftError } from "@/server/pos-shift";export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(request:Request){try{const input=readCurrentShiftInput(new URL(request.url).searchParams);const shift=await shiftCashRepository.current(input);return ok({shift},undefined,{endpoint:"GET /api/pos/shift/current",businessId:input.businessId,terminalId:input.terminalId});}catch(error){if(error instanceof ShiftError)return fail(error.code,error.message,error.status,error.details);return fail("SHIFT_API_INTERNAL_ERROR","Error interno al consultar turno.",500);}}
