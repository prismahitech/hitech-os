import{toPosApiError}from"@/server/pos-api/errors";import{ok}from"@/server/pos-api/responses";import{requestPendingRetry}from"@/server/pos-sync-panel";export const runtime="nodejs";export const dynamic="force-dynamic";export async function POST(request:Request){try{const body=await request.json().catch(()=>({}));const businessId=typeof body.businessId==="string"?body.businessId:"biz_demo_001";const ids=Array.isArray(body.ids)?body.ids.filter((id:unknown)=>typeof id==="string"):undefined;const result=await requestPendingRetry({businessId,ids,includeFailed:body.includeFailed!==false,includePending:body.includePending===true});return ok(result,undefined,{endpoint:"POST /api/pos/sync/retry",businessId,message:result.message})}catch(error){return toPosApiError(error)}}
