import { fail, ok } from "@/server/pos-api/responses";import { readOpenShiftInput, shiftCashRepository, ShiftError } from "@/server/pos-shift";export const runtime="nodejs";export const dynamic="force-dynamic";export async function POST(request:Request){try{const input=await readOpenShiftInput(request);const shift=await shiftCashRepository.open(input);return ok({shift},{status:201},{endpoint:"POST /api/pos/shift/open",businessId:input.businessId,terminalId:input.terminalId,shiftId:shift.id});}catch(error){if(error instanceof ShiftError)return fail(error.code,error.message,error.status,error.details);return fail("SHIFT_API_INTERNAL_ERROR","Error interno al abrir turno.",500);}}
