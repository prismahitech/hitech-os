import { fail, ok } from "@/server/pos-api/responses";import { readCloseShiftInput, shiftCashRepository, ShiftError } from "@/server/pos-shift";export const runtime="nodejs";export const dynamic="force-dynamic";export async function POST(request:Request){try{const input=await readCloseShiftInput(request);const shift=await shiftCashRepository.close(input);return ok({shift},undefined,{endpoint:"POST /api/pos/shift/close",businessId:input.businessId,terminalId:input.terminalId,shiftId:shift.id,varianceCents:shift.varianceCents});}catch(error){if(error instanceof ShiftError)return fail(error.code,error.message,error.status,error.details);return fail("SHIFT_API_INTERNAL_ERROR","Error interno al cerrar turno.",500);}}
