#!/usr/bin/env node
import fs from "node:fs";import path from "node:path";const root=process.argv.includes("--root")?process.argv[process.argv.indexOf("--root")+1]:process.cwd();const manifest=JSON.parse(fs.readFileSync(path.join(root,'tools/fixtures/tablet_operable_release_gate_capture_manifest_03z.json'),'utf8'));if(manifest.captures.length<manifest.minimumCaptures)throw new Error('capture manifest below minimum');for(const c of manifest.captures){if(!c.route||!c.fileName||!c.description)throw new Error(`bad capture ${c.id}`)}console.log('OK expected captures',manifest.captures.length);console.log('READY 03Z capture manifest');
