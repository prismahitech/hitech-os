import fs from "node:fs";import path from "node:path";import zlib from "node:zlib";function argRoot(){const i=process.argv.indexOf("--root");return i>=0?process.argv[i+1]:process.cwd();}const root=argRoot();function read(rel){return fs.readFileSync(path.join(root,rel),"utf8");}function exists(rel){if(!fs.existsSync(path.join(root,rel)))throw new Error(`missing ${rel}`);}function has(rel,needle){const text=read(rel);if(!text.includes(needle))throw new Error(`missing ${needle} in ${rel}`);}function json(rel){return JSON.parse(read(rel));}
const files=["app/sync/page.tsx","components/sync/pending-offline-sync-panel-screen.tsx","src/server/pos-sync-panel/index.ts","app/api/pos/sync/panel/route.ts","app/api/pos/sync/retry/route.ts","src/lib/pending-offline-sync/sync-panel-view-model.ts","components/tablet-shell/tablet-nav.ts"];for(const f of files)exists(f);has("app/sync/page.tsx","PendingOfflineSyncPanelScreen");has("components/sync/pending-offline-sync-panel-screen.tsx","Pendientes y conexión");has("src/server/pos-sync-panel/index.ts","requestPendingRetry");has("components/tablet-shell/tablet-nav.ts","Pendientes");console.log("OK 03M static files",files.length);console.log("READY pending offline sync panel");
