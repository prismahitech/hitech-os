#!/usr/bin/env node
import fs from "node:fs";import path from "node:path";const root=process.argv.includes("--root")?process.argv[process.argv.indexOf("--root")+1]:process.cwd();const graph=JSON.parse(fs.readFileSync(path.join(root,"tools/fixtures/tablet_operable_release_gate_route_graph_03z.json"),"utf8"));const routeToFile=r=>r==='/'?'app/page.tsx':`app${r}/page.tsx`;for(const item of graph.requiredRoutes){const rel=routeToFile(item.route);if(!fs.existsSync(path.join(root,rel)))throw new Error(`missing route ${item.route} -> ${rel}`)}console.log('OK release route graph',graph.requiredRoutes.length);console.log('READY 03Z route graph');
