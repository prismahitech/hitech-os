import fs from "node:fs";import path from "node:path";import zlib from "node:zlib";function argRoot(){const i=process.argv.indexOf("--root");return i>=0?process.argv[i+1]:process.cwd();}const root=argRoot();function read(rel){return fs.readFileSync(path.join(root,rel),"utf8");}function exists(rel){if(!fs.existsSync(path.join(root,rel)))throw new Error(`missing ${rel}`);}function has(rel,needle){const text=read(rel);if(!text.includes(needle))throw new Error(`missing ${needle} in ${rel}`);}
const data=JSON.parse(read("tools/fixtures/tablet_shift_cash_closure_extended_scenarios_03l.json"));if(!Array.isArray(data))throw new Error("fixture must be array");if(data.length<120)throw new Error(`fixture too small ${data.length}`);const kinds=new Set(data.map(x=>x.kind));if(kinds.size<4)throw new Error("not enough scenario kinds");console.log("OK tablet_shift_cash_closure_extended_scenarios_03l.json",data.length);console.log("OK buckets",JSON.stringify(Object.fromEntries([...kinds].map(k=>[k,data.filter(x=>x.kind===k).length]))));console.log("READY shift cash closure scenario matrix");
