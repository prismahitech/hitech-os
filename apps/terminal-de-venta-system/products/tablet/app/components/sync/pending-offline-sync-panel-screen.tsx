"use client";import{useEffect,useMemo,useState}from"react";import{PrismaTabletShellUnified,TabletShellStatusPill}from"@components/tablet-shell/prisma-tablet-shell";import{requestJson}from"@/lib/pos/cart-state";import type{PendingSendStatus,SyncPanelResponse}from"@/lib/pending-offline-sync/sync-panel-contract";import{filterSyncItems}from"@/lib/pending-offline-sync/sync-panel-view-model";import styles from"./pending-offline-sync-panel.module.css";type FilterMode="all"|"needs_attention"|PendingSendStatus;const FILTERS:{key:FilterMode;label:string}[]=[{key:"needs_attention",label:"Por atender"},{key:"pending",label:"Pendientes"},{key:"failed",label:"Fallidos"},{key:"conflict",label:"Revisión"},{key:"all",label:"Todo"}];export function PendingOfflineSyncPanelScreen(){const[panel,setPanel]=useState<SyncPanelResponse|null>(null);const[filter,setFilter]=useState<FilterMode>("needs_attention");const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);async function load(){setBusy(true);setError(null);try{const r=await requestJson<SyncPanelResponse>("/api/pos/sync/panel?limit=120");setPanel(r.data)}catch(e){setError(readError(e))}finally{setBusy(false)}}useEffect(()=>{void load()},[]);async function retryFailed(){setBusy(true);setError(null);try{await requestJson("/api/pos/sync/retry",{method:"POST",body:JSON.stringify({includeFailed:true,includePending:false})});await load()}catch(e){setError(readError(e))}finally{setBusy(false)}}const items=useMemo(()=>panel?filterSyncItems(panel.items,filter):[],[panel,filter]);const tone=panel?.summary.risk==="danger"?"danger":panel?.summary.risk==="warn"?"warn":"ok";return <PrismaTabletShellUnified currentPath="/sync" title="Pendientes y conexión" subtitle="Lo que quedó guardado localmente, lo que falló y lo que necesita atención." status={<TabletShellStatusPill tone={tone}>{panel?.summary.headline??"Revisando"}</TabletShellStatusPill>}><main className={styles.page}><section className={styles.hero}><div><span>Continuidad operativa</span><h1>{panel?.summary.operatorMessage??"Revisando trabajo local"}</h1><p>La Tablet puede seguir vendiendo; aquí ves qué falta por enviar o revisar, sin cables pelados en la cara del cajero.</p></div><div className={styles.heroActions}><button type="button" onClick={()=>void load()} disabled={busy}>Actualizar</button><button type="button" onClick={()=>void retryFailed()} disabled={busy||!panel||panel.summary.failed+panel.summary.conflict===0}>Reintentar fallidos</button></div></section>{error?<div className={styles.alert} role="alert">{error}</div>:null}<section className={styles.kpis}><article><span>Pendientes</span><strong>{panel?.summary.pending??0}</strong><small>Guardados para enviar</small></article><article><span>Fallidos</span><strong>{panel?.summary.failed??0}</strong><small>Requieren reintento</small></article><article><span>Revisión</span><strong>{panel?.summary.conflict??0}</strong><small>Atención antes de enviar</small></article><article><span>Confirmados</span><strong>{panel?.summary.acked??0}</strong><small>Ya quedaron cerrados</small></article></section><section className={styles.filterBar}>{FILTERS.map(f=><button key={f.key} type="button" onClick={()=>setFilter(f.key)} className={filter===f.key?styles.activeFilter:undefined}>{f.label}</button>)}</section><section className={styles.queue}>{items.length===0?<div className={styles.empty}>No hay elementos en este filtro. Extraño, casi sospechosamente ordenado.</div>:items.map(item=><article className={[styles.item,styles[`risk_${item.risk}`]].join(" ")} key={item.id}><div><span>{item.statusLabel}</span><h2>{item.title}</h2><p>{item.description}</p></div><aside><strong>{item.attempts}</strong><small>intentos</small>{item.canRetry?<em>Reintento disponible</em>:<em>Sin acción requerida</em>}</aside></article>)}</section><section className={styles.diagnostics}>{panel?.diagnostics.map(note=><span key={note}>{note}</span>)}</section></main></PrismaTabletShellUnified>}function readError(error:unknown){return error instanceof Error?error.message:"No se pudo revisar pendientes."}
