"use client";import{formatMoney}from"@/lib/pos/cart-state";import type{SalesTodayTicket}from"@/lib/sales-today/types";import{formatTicketTime}from"@/lib/sales-today/view-model";import styles from"./sales.module.css";export function SalesTicketList({tickets}:{tickets:SalesTodayTicket[]}){if(!tickets.length)return <div className={styles.empty}>Aún no hay tickets cerrados para mostrar.</div>;return <section>{tickets.map(t=><a className={styles.ticketRow} href={`/sales/today/${encodeURIComponent(t.saleId)}`} key={t.saleId}><div><strong>{t.folio}</strong><span>{formatTicketTime(t.createdAt)} · {t.cashier}</span></div><strong>{formatMoney(t.totalCents)}</strong><span>Ver detalle</span></a>)}</section>}
