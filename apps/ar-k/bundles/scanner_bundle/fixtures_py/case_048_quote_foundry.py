from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_048_quote_foundry',
    description="Quote Foundry synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('homologated', 'observed_only', 'plaza', 'quote_foundry', 'resilient', 'scanner'),
    source_files={'app/quote_foundry/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.quote_foundry.worker import build_quote_foundry_packet\n\n@dataclass\nclass QuoteFoundryService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_quote_foundry_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'quote_foundry'\n        packet['profile'] = 'resilient'\n        return packet\n\ndef load_quote_foundry_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'plaza'\n    return data\n", 'app/quote_foundry/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_quote_foundry_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"quote_foundry:{tenant}:48".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(48 / 3),\n        \'lane\': \'resilient\',\n    }\n\nclass QuoteFoundryWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_quote_foundry_packet(self.tenant)\n', 'app/quote_foundry/routes.py': "from __future__ import annotations\nROUTES = [\n    '/quote-foundry/resilient-048',\n    '/quote-foundry/resilient-048/details',\n    '/quote-foundry/resilient-048/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { QuoteFoundryScreen } from "./pages/QuoteFoundryScreen";\nimport { registerQuoteFoundryRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/QuoteFoundryBridge";\n\nregisterQuoteFoundryRoutes();\nbridgeToDesktop({ lane: \'resilient\', docket: \'plaza\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <QuoteFoundryScreen mode="resilient" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { QuoteFoundryScreen } from "../pages/QuoteFoundryScreen";\n\nexport function registerQuoteFoundryRoutes() {\n    return [\n        { path: \'/quote-foundry/resilient-048\', element: <QuoteFoundryScreen mode="home" /> },\n        { path: \'/quote-foundry/resilient-048/details\', element: <QuoteFoundryScreen mode="details" /> },\n        { path: \'/quote-foundry/resilient-048/history\', element: <QuoteFoundryScreen mode="history" /> },\n    ];\n}\n', 'src/pages/QuoteFoundryScreen.tsx': 'import React from "react";\nimport { QuoteFoundryPanel } from "../components/QuoteFoundryPanel";\nimport { QuoteFoundryStats } from "../components/QuoteFoundryStats";\n\nexport function QuoteFoundryScreen(props: { mode: string }) {\n    return (\n        <section data-domain="quote_foundry" data-mode={props.mode}>\n            <QuoteFoundryPanel heading="Quote Foundry" />\n            <QuoteFoundryStats profile="resilient" channel="plaza" />\n        </section>\n    );\n}\n', 'src/components/QuoteFoundryPanel.tsx': 'import React from "react";\nexport function QuoteFoundryPanel(props: { heading: string }) {\n    return <div className="panel-048">{props.heading} for quote_foundry</div>;\n}\n', 'src/components/QuoteFoundryStats.tsx': 'import React from "react";\nexport function QuoteFoundryStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 048</aside>;\n}\n', 'src/bridges/QuoteFoundryBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "QuoteFoundryBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "quote_foundry", "mode": "resilient", "boundary": "plaza"},\n    {"name": "quote_foundry_detail", "mode": "detail", "boundary": "plaza_detail"}\n  ],\n  "legacy_index": "query_index.json"\n}\n', 'docs/quote_foundry_architecture.md': '# Quote Foundry architecture\nThe quote_foundry fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /quote-foundry/resilient-048\nBoundary hint: plaza\nProfile: resilient\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/quote_foundry.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("quote_foundry", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/quote-foundry/resilient-048").toContain("quote");\n    });\n});\n', 'tools/enforce_quote_foundry_policy.py': "from __future__ import annotations\ndef verify_quote_foundry_policy() -> tuple[str, str]:\n    return ('resilient', 'plaza')\n", 'scripts/rebuild_quote_foundry.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild quote_foundry in resilient mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/quote_foundry_seed.py': "SEED = {'domain': 'quote_foundry', 'profile': 'resilient', 'channel': 'plaza', 'id': 48}\n", 'examples/quote_foundry_example.py': "EXAMPLE = {'path': '/quote-foundry/resilient-048', 'screen': 'QuoteFoundryScreen', 'legacy_query_index': True}\n", 'reports/quote_foundry_run.md': 'run record for quote_foundry should be excluded by shared canon.\n', 'reports_real/quote_foundry_live.md': 'live report for quote_foundry should be excluded by shared canon.\n', 'generated/quote_foundry/snapshot.json': '{"domain": "quote_foundry", "generated": true, "profile": "resilient"}\n', 'runtime/quote_foundry/stream.log': 'quote_foundry runtime marker 048\n', '.shadow_resilient_plaza_048.md': 'hidden note for quote_foundry resilient plaza 048\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': True},
)
