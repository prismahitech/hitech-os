from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_087_forecast_bloom',
    description="Forecast Bloom synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('atlas', 'forecast_bloom', 'guarded', 'homologated', 'observed_only', 'scanner'),
    source_files={'app/forecast_bloom/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.forecast_bloom.worker import build_forecast_bloom_packet\n\n@dataclass\nclass ForecastBloomService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_forecast_bloom_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'forecast_bloom'\n        packet['profile'] = 'guarded'\n        return packet\n\ndef load_forecast_bloom_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'atlas'\n    return data\n", 'app/forecast_bloom/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_forecast_bloom_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"forecast_bloom:{tenant}:87".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(87 / 3),\n        \'lane\': \'guarded\',\n    }\n\nclass ForecastBloomWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_forecast_bloom_packet(self.tenant)\n', 'app/forecast_bloom/routes.py': "from __future__ import annotations\nROUTES = [\n    '/forecast-bloom/guarded-087',\n    '/forecast-bloom/guarded-087/details',\n    '/forecast-bloom/guarded-087/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { ForecastBloomScreen } from "./pages/ForecastBloomScreen";\nimport { registerForecastBloomRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/ForecastBloomBridge";\n\nregisterForecastBloomRoutes();\nbridgeToDesktop({ lane: \'guarded\', docket: \'atlas\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <ForecastBloomScreen mode="guarded" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { ForecastBloomScreen } from "../pages/ForecastBloomScreen";\n\nexport function registerForecastBloomRoutes() {\n    return [\n        { path: \'/forecast-bloom/guarded-087\', element: <ForecastBloomScreen mode="home" /> },\n        { path: \'/forecast-bloom/guarded-087/details\', element: <ForecastBloomScreen mode="details" /> },\n        { path: \'/forecast-bloom/guarded-087/history\', element: <ForecastBloomScreen mode="history" /> },\n    ];\n}\n', 'src/pages/ForecastBloomScreen.tsx': 'import React from "react";\nimport { ForecastBloomPanel } from "../components/ForecastBloomPanel";\nimport { ForecastBloomStats } from "../components/ForecastBloomStats";\n\nexport function ForecastBloomScreen(props: { mode: string }) {\n    return (\n        <section data-domain="forecast_bloom" data-mode={props.mode}>\n            <ForecastBloomPanel heading="Forecast Bloom" />\n            <ForecastBloomStats profile="guarded" channel="atlas" />\n        </section>\n    );\n}\n', 'src/components/ForecastBloomPanel.tsx': 'import React from "react";\nexport function ForecastBloomPanel(props: { heading: string }) {\n    return <div className="panel-087">{props.heading} for forecast_bloom</div>;\n}\n', 'src/components/ForecastBloomStats.tsx': 'import React from "react";\nexport function ForecastBloomStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 087</aside>;\n}\n', 'src/bridges/ForecastBloomBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "ForecastBloomBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "forecast_bloom", "mode": "guarded", "boundary": "atlas"},\n    {"name": "forecast_bloom_detail", "mode": "detail", "boundary": "atlas_detail"}\n  ],\n  "legacy_index": "registry_index.json"\n}\n', 'docs/forecast_bloom_architecture.md': '# Forecast Bloom architecture\nThe forecast_bloom fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /forecast-bloom/guarded-087\nBoundary hint: atlas\nProfile: guarded\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/forecast_bloom.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("forecast_bloom", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/forecast-bloom/guarded-087").toContain("forecast");\n    });\n});\n', 'tools/enforce_forecast_bloom_policy.py': "from __future__ import annotations\ndef verify_forecast_bloom_policy() -> tuple[str, str]:\n    return ('guarded', 'atlas')\n", 'scripts/rebuild_forecast_bloom.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild forecast_bloom in guarded mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/forecast_bloom_seed.py': "SEED = {'domain': 'forecast_bloom', 'profile': 'guarded', 'channel': 'atlas', 'id': 87}\n", 'examples/forecast_bloom_example.py': "EXAMPLE = {'path': '/forecast-bloom/guarded-087', 'screen': 'ForecastBloomScreen', 'legacy_query_index': False}\n", 'reports/forecast_bloom_run.md': 'run record for forecast_bloom should be excluded by shared canon.\n', 'reports_real/forecast_bloom_live.md': 'live report for forecast_bloom should be excluded by shared canon.\n', 'generated/forecast_bloom/snapshot.json': '{"domain": "forecast_bloom", "generated": true, "profile": "guarded"}\n', 'runtime/forecast_bloom/stream.log': 'forecast_bloom runtime marker 087\n', '.shadow_guarded_atlas_087.md': 'hidden note for forecast_bloom guarded atlas 087\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': False},
)
