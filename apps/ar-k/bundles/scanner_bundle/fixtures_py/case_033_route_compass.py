from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_033_route_compass',
    description="Route Compass synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('curated', 'homologated', 'kernel', 'observed_only', 'route_compass', 'scanner'),
    source_files={'app/route_compass/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.route_compass.worker import build_route_compass_packet\n\n@dataclass\nclass RouteCompassService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_route_compass_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'route_compass'\n        packet['profile'] = 'curated'\n        return packet\n\ndef load_route_compass_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'kernel'\n    return data\n", 'app/route_compass/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_route_compass_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"route_compass:{tenant}:33".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(33 / 3),\n        \'lane\': \'curated\',\n    }\n\nclass RouteCompassWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_route_compass_packet(self.tenant)\n', 'app/route_compass/routes.py': "from __future__ import annotations\nROUTES = [\n    '/route-compass/curated-033',\n    '/route-compass/curated-033/details',\n    '/route-compass/curated-033/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { RouteCompassScreen } from "./pages/RouteCompassScreen";\nimport { registerRouteCompassRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/RouteCompassBridge";\n\nregisterRouteCompassRoutes();\nbridgeToDesktop({ lane: \'curated\', docket: \'kernel\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <RouteCompassScreen mode="curated" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { RouteCompassScreen } from "../pages/RouteCompassScreen";\n\nexport function registerRouteCompassRoutes() {\n    return [\n        { path: \'/route-compass/curated-033\', element: <RouteCompassScreen mode="home" /> },\n        { path: \'/route-compass/curated-033/details\', element: <RouteCompassScreen mode="details" /> },\n        { path: \'/route-compass/curated-033/history\', element: <RouteCompassScreen mode="history" /> },\n    ];\n}\n', 'src/pages/RouteCompassScreen.tsx': 'import React from "react";\nimport { RouteCompassPanel } from "../components/RouteCompassPanel";\nimport { RouteCompassStats } from "../components/RouteCompassStats";\n\nexport function RouteCompassScreen(props: { mode: string }) {\n    return (\n        <section data-domain="route_compass" data-mode={props.mode}>\n            <RouteCompassPanel heading="Route Compass" />\n            <RouteCompassStats profile="curated" channel="kernel" />\n        </section>\n    );\n}\n', 'src/components/RouteCompassPanel.tsx': 'import React from "react";\nexport function RouteCompassPanel(props: { heading: string }) {\n    return <div className="panel-033">{props.heading} for route_compass</div>;\n}\n', 'src/components/RouteCompassStats.tsx': 'import React from "react";\nexport function RouteCompassStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 033</aside>;\n}\n', 'src/bridges/RouteCompassBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "RouteCompassBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "route_compass", "mode": "curated", "boundary": "kernel"},\n    {"name": "route_compass_detail", "mode": "detail", "boundary": "kernel_detail"}\n  ],\n  "legacy_index": "registry_index.json"\n}\n', 'docs/route_compass_architecture.md': '# Route Compass architecture\nThe route_compass fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /route-compass/curated-033\nBoundary hint: kernel\nProfile: curated\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/route_compass.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("route_compass", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/route-compass/curated-033").toContain("route");\n    });\n});\n', 'tools/enforce_route_compass_policy.py': "from __future__ import annotations\ndef verify_route_compass_policy() -> tuple[str, str]:\n    return ('curated', 'kernel')\n", 'scripts/rebuild_route_compass.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild route_compass in curated mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/route_compass_seed.py': "SEED = {'domain': 'route_compass', 'profile': 'curated', 'channel': 'kernel', 'id': 33}\n", 'examples/route_compass_example.py': "EXAMPLE = {'path': '/route-compass/curated-033', 'screen': 'RouteCompassScreen', 'legacy_query_index': False}\n", 'reports/route_compass_run.md': 'run record for route_compass should be excluded by shared canon.\n', 'reports_real/route_compass_live.md': 'live report for route_compass should be excluded by shared canon.\n', 'generated/route_compass/snapshot.json': '{"domain": "route_compass", "generated": true, "profile": "curated"}\n', 'runtime/route_compass/stream.log': 'route_compass runtime marker 033\n', '.shadow_curated_kernel_033.md': 'hidden note for route_compass curated kernel 033\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': False},
)
