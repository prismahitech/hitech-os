from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_028_ledger_tunnel',
    description="Ledger Tunnel synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('homologated', 'ledger_tunnel', 'matrix', 'observed_only', 'resilient', 'scanner'),
    source_files={'app/ledger_tunnel/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.ledger_tunnel.worker import build_ledger_tunnel_packet\n\n@dataclass\nclass LedgerTunnelService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_ledger_tunnel_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'ledger_tunnel'\n        packet['profile'] = 'resilient'\n        return packet\n\ndef load_ledger_tunnel_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'matrix'\n    return data\n", 'app/ledger_tunnel/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_ledger_tunnel_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"ledger_tunnel:{tenant}:28".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(28 / 3),\n        \'lane\': \'resilient\',\n    }\n\nclass LedgerTunnelWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_ledger_tunnel_packet(self.tenant)\n', 'app/ledger_tunnel/routes.py': "from __future__ import annotations\nROUTES = [\n    '/ledger-tunnel/resilient-028',\n    '/ledger-tunnel/resilient-028/details',\n    '/ledger-tunnel/resilient-028/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { LedgerTunnelScreen } from "./pages/LedgerTunnelScreen";\nimport { registerLedgerTunnelRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/LedgerTunnelBridge";\n\nregisterLedgerTunnelRoutes();\nbridgeToDesktop({ lane: \'resilient\', docket: \'matrix\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <LedgerTunnelScreen mode="resilient" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { LedgerTunnelScreen } from "../pages/LedgerTunnelScreen";\n\nexport function registerLedgerTunnelRoutes() {\n    return [\n        { path: \'/ledger-tunnel/resilient-028\', element: <LedgerTunnelScreen mode="home" /> },\n        { path: \'/ledger-tunnel/resilient-028/details\', element: <LedgerTunnelScreen mode="details" /> },\n        { path: \'/ledger-tunnel/resilient-028/history\', element: <LedgerTunnelScreen mode="history" /> },\n    ];\n}\n', 'src/pages/LedgerTunnelScreen.tsx': 'import React from "react";\nimport { LedgerTunnelPanel } from "../components/LedgerTunnelPanel";\nimport { LedgerTunnelStats } from "../components/LedgerTunnelStats";\n\nexport function LedgerTunnelScreen(props: { mode: string }) {\n    return (\n        <section data-domain="ledger_tunnel" data-mode={props.mode}>\n            <LedgerTunnelPanel heading="Ledger Tunnel" />\n            <LedgerTunnelStats profile="resilient" channel="matrix" />\n        </section>\n    );\n}\n', 'src/components/LedgerTunnelPanel.tsx': 'import React from "react";\nexport function LedgerTunnelPanel(props: { heading: string }) {\n    return <div className="panel-028">{props.heading} for ledger_tunnel</div>;\n}\n', 'src/components/LedgerTunnelStats.tsx': 'import React from "react";\nexport function LedgerTunnelStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 028</aside>;\n}\n', 'src/bridges/LedgerTunnelBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "LedgerTunnelBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "ledger_tunnel", "mode": "resilient", "boundary": "matrix"},\n    {"name": "ledger_tunnel_detail", "mode": "detail", "boundary": "matrix_detail"}\n  ],\n  "legacy_index": "query_index.json"\n}\n', 'docs/ledger_tunnel_architecture.md': '# Ledger Tunnel architecture\nThe ledger_tunnel fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /ledger-tunnel/resilient-028\nBoundary hint: matrix\nProfile: resilient\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/ledger_tunnel.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("ledger_tunnel", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/ledger-tunnel/resilient-028").toContain("ledger");\n    });\n});\n', 'tools/enforce_ledger_tunnel_policy.py': "from __future__ import annotations\ndef verify_ledger_tunnel_policy() -> tuple[str, str]:\n    return ('resilient', 'matrix')\n", 'scripts/rebuild_ledger_tunnel.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild ledger_tunnel in resilient mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/ledger_tunnel_seed.py': "SEED = {'domain': 'ledger_tunnel', 'profile': 'resilient', 'channel': 'matrix', 'id': 28}\n", 'examples/ledger_tunnel_example.py': "EXAMPLE = {'path': '/ledger-tunnel/resilient-028', 'screen': 'LedgerTunnelScreen', 'legacy_query_index': True}\n", 'reports/ledger_tunnel_run.md': 'run record for ledger_tunnel should be excluded by shared canon.\n', 'reports_real/ledger_tunnel_live.md': 'live report for ledger_tunnel should be excluded by shared canon.\n', 'generated/ledger_tunnel/snapshot.json': '{"domain": "ledger_tunnel", "generated": true, "profile": "resilient"}\n', 'runtime/ledger_tunnel/stream.log': 'ledger_tunnel runtime marker 028\n', '.shadow_resilient_matrix_028.md': 'hidden note for ledger_tunnel resilient matrix 028\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': True},
)
