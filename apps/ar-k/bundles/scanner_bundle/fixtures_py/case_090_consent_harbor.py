from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_090_consent_harbor',
    description="Consent Harbor synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('bounded', 'bridge', 'consent_harbor', 'homologated', 'observed_only', 'scanner'),
    source_files={'app/consent_harbor/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.consent_harbor.worker import build_consent_harbor_packet\n\n@dataclass\nclass ConsentHarborService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_consent_harbor_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'consent_harbor'\n        packet['profile'] = 'bounded'\n        return packet\n\ndef load_consent_harbor_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'bridge'\n    return data\n", 'app/consent_harbor/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_consent_harbor_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"consent_harbor:{tenant}:90".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(90 / 3),\n        \'lane\': \'bounded\',\n    }\n\nclass ConsentHarborWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_consent_harbor_packet(self.tenant)\n', 'app/consent_harbor/routes.py': "from __future__ import annotations\nROUTES = [\n    '/consent-harbor/bounded-090',\n    '/consent-harbor/bounded-090/details',\n    '/consent-harbor/bounded-090/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { ConsentHarborScreen } from "./pages/ConsentHarborScreen";\nimport { registerConsentHarborRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/ConsentHarborBridge";\n\nregisterConsentHarborRoutes();\nbridgeToDesktop({ lane: \'bounded\', docket: \'bridge\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <ConsentHarborScreen mode="bounded" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { ConsentHarborScreen } from "../pages/ConsentHarborScreen";\n\nexport function registerConsentHarborRoutes() {\n    return [\n        { path: \'/consent-harbor/bounded-090\', element: <ConsentHarborScreen mode="home" /> },\n        { path: \'/consent-harbor/bounded-090/details\', element: <ConsentHarborScreen mode="details" /> },\n        { path: \'/consent-harbor/bounded-090/history\', element: <ConsentHarborScreen mode="history" /> },\n    ];\n}\n', 'src/pages/ConsentHarborScreen.tsx': 'import React from "react";\nimport { ConsentHarborPanel } from "../components/ConsentHarborPanel";\nimport { ConsentHarborStats } from "../components/ConsentHarborStats";\n\nexport function ConsentHarborScreen(props: { mode: string }) {\n    return (\n        <section data-domain="consent_harbor" data-mode={props.mode}>\n            <ConsentHarborPanel heading="Consent Harbor" />\n            <ConsentHarborStats profile="bounded" channel="bridge" />\n        </section>\n    );\n}\n', 'src/components/ConsentHarborPanel.tsx': 'import React from "react";\nexport function ConsentHarborPanel(props: { heading: string }) {\n    return <div className="panel-090">{props.heading} for consent_harbor</div>;\n}\n', 'src/components/ConsentHarborStats.tsx': 'import React from "react";\nexport function ConsentHarborStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 090</aside>;\n}\n', 'src/bridges/ConsentHarborBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "ConsentHarborBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "consent_harbor", "mode": "bounded", "boundary": "bridge"},\n    {"name": "consent_harbor_detail", "mode": "detail", "boundary": "bridge_detail"}\n  ],\n  "legacy_index": "registry_index.json"\n}\n', 'docs/consent_harbor_architecture.md': '# Consent Harbor architecture\nThe consent_harbor fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /consent-harbor/bounded-090\nBoundary hint: bridge\nProfile: bounded\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/consent_harbor.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("consent_harbor", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/consent-harbor/bounded-090").toContain("consent");\n    });\n});\n', 'tools/enforce_consent_harbor_policy.py': "from __future__ import annotations\ndef verify_consent_harbor_policy() -> tuple[str, str]:\n    return ('bounded', 'bridge')\n", 'scripts/rebuild_consent_harbor.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild consent_harbor in bounded mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/consent_harbor_seed.py': "SEED = {'domain': 'consent_harbor', 'profile': 'bounded', 'channel': 'bridge', 'id': 90}\n", 'examples/consent_harbor_example.py': "EXAMPLE = {'path': '/consent-harbor/bounded-090', 'screen': 'ConsentHarborScreen', 'legacy_query_index': False}\n", 'reports/consent_harbor_run.md': 'run record for consent_harbor should be excluded by shared canon.\n', 'reports_real/consent_harbor_live.md': 'live report for consent_harbor should be excluded by shared canon.\n', 'generated/consent_harbor/snapshot.json': '{"domain": "consent_harbor", "generated": true, "profile": "bounded"}\n', 'runtime/consent_harbor/stream.log': 'consent_harbor runtime marker 090\n', '.shadow_bounded_bridge_090.md': 'hidden note for consent_harbor bounded bridge 090\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': False},
)
