from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_040_latency_watch',
    description="Latency Watch synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('homologated', 'latency_watch', 'observed_only', 'scanner', 'screened', 'tunnel'),
    source_files={'app/latency_watch/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.latency_watch.worker import build_latency_watch_packet\n\n@dataclass\nclass LatencyWatchService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_latency_watch_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'latency_watch'\n        packet['profile'] = 'screened'\n        return packet\n\ndef load_latency_watch_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'tunnel'\n    return data\n", 'app/latency_watch/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_latency_watch_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"latency_watch:{tenant}:40".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(40 / 3),\n        \'lane\': \'screened\',\n    }\n\nclass LatencyWatchWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_latency_watch_packet(self.tenant)\n', 'app/latency_watch/routes.py': "from __future__ import annotations\nROUTES = [\n    '/latency-watch/screened-040',\n    '/latency-watch/screened-040/details',\n    '/latency-watch/screened-040/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { LatencyWatchScreen } from "./pages/LatencyWatchScreen";\nimport { registerLatencyWatchRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/LatencyWatchBridge";\n\nregisterLatencyWatchRoutes();\nbridgeToDesktop({ lane: \'screened\', docket: \'tunnel\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <LatencyWatchScreen mode="screened" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { LatencyWatchScreen } from "../pages/LatencyWatchScreen";\n\nexport function registerLatencyWatchRoutes() {\n    return [\n        { path: \'/latency-watch/screened-040\', element: <LatencyWatchScreen mode="home" /> },\n        { path: \'/latency-watch/screened-040/details\', element: <LatencyWatchScreen mode="details" /> },\n        { path: \'/latency-watch/screened-040/history\', element: <LatencyWatchScreen mode="history" /> },\n    ];\n}\n', 'src/pages/LatencyWatchScreen.tsx': 'import React from "react";\nimport { LatencyWatchPanel } from "../components/LatencyWatchPanel";\nimport { LatencyWatchStats } from "../components/LatencyWatchStats";\n\nexport function LatencyWatchScreen(props: { mode: string }) {\n    return (\n        <section data-domain="latency_watch" data-mode={props.mode}>\n            <LatencyWatchPanel heading="Latency Watch" />\n            <LatencyWatchStats profile="screened" channel="tunnel" />\n        </section>\n    );\n}\n', 'src/components/LatencyWatchPanel.tsx': 'import React from "react";\nexport function LatencyWatchPanel(props: { heading: string }) {\n    return <div className="panel-040">{props.heading} for latency_watch</div>;\n}\n', 'src/components/LatencyWatchStats.tsx': 'import React from "react";\nexport function LatencyWatchStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 040</aside>;\n}\n', 'src/bridges/LatencyWatchBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "LatencyWatchBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "latency_watch", "mode": "screened", "boundary": "tunnel"},\n    {"name": "latency_watch_detail", "mode": "detail", "boundary": "tunnel_detail"}\n  ],\n  "legacy_index": "query_index.json"\n}\n', 'docs/latency_watch_architecture.md': '# Latency Watch architecture\nThe latency_watch fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /latency-watch/screened-040\nBoundary hint: tunnel\nProfile: screened\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/latency_watch.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("latency_watch", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/latency-watch/screened-040").toContain("latency");\n    });\n});\n', 'tools/enforce_latency_watch_policy.py': "from __future__ import annotations\ndef verify_latency_watch_policy() -> tuple[str, str]:\n    return ('screened', 'tunnel')\n", 'scripts/rebuild_latency_watch.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild latency_watch in screened mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/latency_watch_seed.py': "SEED = {'domain': 'latency_watch', 'profile': 'screened', 'channel': 'tunnel', 'id': 40}\n", 'examples/latency_watch_example.py': "EXAMPLE = {'path': '/latency-watch/screened-040', 'screen': 'LatencyWatchScreen', 'legacy_query_index': True}\n", 'reports/latency_watch_run.md': 'run record for latency_watch should be excluded by shared canon.\n', 'reports_real/latency_watch_live.md': 'live report for latency_watch should be excluded by shared canon.\n', 'generated/latency_watch/snapshot.json': '{"domain": "latency_watch", "generated": true, "profile": "screened"}\n', 'runtime/latency_watch/stream.log': 'latency_watch runtime marker 040\n', '.shadow_screened_tunnel_040.md': 'hidden note for latency_watch screened tunnel 040\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': True},
)
