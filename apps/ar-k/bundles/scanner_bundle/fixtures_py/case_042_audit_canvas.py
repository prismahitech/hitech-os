from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_042_audit_canvas',
    description="Audit Canvas synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('audit_canvas', 'bridge', 'granular', 'homologated', 'observed_only', 'scanner'),
    source_files={'app/audit_canvas/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.audit_canvas.worker import build_audit_canvas_packet\n\n@dataclass\nclass AuditCanvasService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_audit_canvas_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'audit_canvas'\n        packet['profile'] = 'granular'\n        return packet\n\ndef load_audit_canvas_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'bridge'\n    return data\n", 'app/audit_canvas/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_audit_canvas_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"audit_canvas:{tenant}:42".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(42 / 3),\n        \'lane\': \'granular\',\n    }\n\nclass AuditCanvasWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_audit_canvas_packet(self.tenant)\n', 'app/audit_canvas/routes.py': "from __future__ import annotations\nROUTES = [\n    '/audit-canvas/granular-042',\n    '/audit-canvas/granular-042/details',\n    '/audit-canvas/granular-042/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { AuditCanvasScreen } from "./pages/AuditCanvasScreen";\nimport { registerAuditCanvasRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/AuditCanvasBridge";\n\nregisterAuditCanvasRoutes();\nbridgeToDesktop({ lane: \'granular\', docket: \'bridge\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <AuditCanvasScreen mode="granular" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { AuditCanvasScreen } from "../pages/AuditCanvasScreen";\n\nexport function registerAuditCanvasRoutes() {\n    return [\n        { path: \'/audit-canvas/granular-042\', element: <AuditCanvasScreen mode="home" /> },\n        { path: \'/audit-canvas/granular-042/details\', element: <AuditCanvasScreen mode="details" /> },\n        { path: \'/audit-canvas/granular-042/history\', element: <AuditCanvasScreen mode="history" /> },\n    ];\n}\n', 'src/pages/AuditCanvasScreen.tsx': 'import React from "react";\nimport { AuditCanvasPanel } from "../components/AuditCanvasPanel";\nimport { AuditCanvasStats } from "../components/AuditCanvasStats";\n\nexport function AuditCanvasScreen(props: { mode: string }) {\n    return (\n        <section data-domain="audit_canvas" data-mode={props.mode}>\n            <AuditCanvasPanel heading="Audit Canvas" />\n            <AuditCanvasStats profile="granular" channel="bridge" />\n        </section>\n    );\n}\n', 'src/components/AuditCanvasPanel.tsx': 'import React from "react";\nexport function AuditCanvasPanel(props: { heading: string }) {\n    return <div className="panel-042">{props.heading} for audit_canvas</div>;\n}\n', 'src/components/AuditCanvasStats.tsx': 'import React from "react";\nexport function AuditCanvasStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 042</aside>;\n}\n', 'src/bridges/AuditCanvasBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "AuditCanvasBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "audit_canvas", "mode": "granular", "boundary": "bridge"},\n    {"name": "audit_canvas_detail", "mode": "detail", "boundary": "bridge_detail"}\n  ],\n  "legacy_index": "registry_index.json"\n}\n', 'docs/audit_canvas_architecture.md': '# Audit Canvas architecture\nThe audit_canvas fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /audit-canvas/granular-042\nBoundary hint: bridge\nProfile: granular\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/audit_canvas.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("audit_canvas", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/audit-canvas/granular-042").toContain("audit");\n    });\n});\n', 'tools/enforce_audit_canvas_policy.py': "from __future__ import annotations\ndef verify_audit_canvas_policy() -> tuple[str, str]:\n    return ('granular', 'bridge')\n", 'scripts/rebuild_audit_canvas.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild audit_canvas in granular mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/audit_canvas_seed.py': "SEED = {'domain': 'audit_canvas', 'profile': 'granular', 'channel': 'bridge', 'id': 42}\n", 'examples/audit_canvas_example.py': "EXAMPLE = {'path': '/audit-canvas/granular-042', 'screen': 'AuditCanvasScreen', 'legacy_query_index': False}\n", 'reports/audit_canvas_run.md': 'run record for audit_canvas should be excluded by shared canon.\n', 'reports_real/audit_canvas_live.md': 'live report for audit_canvas should be excluded by shared canon.\n', 'generated/audit_canvas/snapshot.json': '{"domain": "audit_canvas", "generated": true, "profile": "granular"}\n', 'runtime/audit_canvas/stream.log': 'audit_canvas runtime marker 042\n', '.shadow_granular_bridge_042.md': 'hidden note for audit_canvas granular bridge 042\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': False},
)
