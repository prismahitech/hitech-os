from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_109_claims_orbit',
    description="Claims Orbit synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('claims_orbit', 'homologated', 'incremental', 'observed_only', 'scanner', 'switch'),
    source_files={'app/claims_orbit/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.claims_orbit.worker import build_claims_orbit_packet\n\n@dataclass\nclass ClaimsOrbitService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_claims_orbit_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'claims_orbit'\n        packet['profile'] = 'incremental'\n        return packet\n\ndef load_claims_orbit_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'switch'\n    return data\n", 'app/claims_orbit/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_claims_orbit_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"claims_orbit:{tenant}:109".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(109 / 3),\n        \'lane\': \'incremental\',\n    }\n\nclass ClaimsOrbitWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_claims_orbit_packet(self.tenant)\n', 'app/claims_orbit/routes.py': "from __future__ import annotations\nROUTES = [\n    '/claims-orbit/incremental-109',\n    '/claims-orbit/incremental-109/details',\n    '/claims-orbit/incremental-109/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { ClaimsOrbitScreen } from "./pages/ClaimsOrbitScreen";\nimport { registerClaimsOrbitRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/ClaimsOrbitBridge";\n\nregisterClaimsOrbitRoutes();\nbridgeToDesktop({ lane: \'incremental\', docket: \'switch\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <ClaimsOrbitScreen mode="incremental" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { ClaimsOrbitScreen } from "../pages/ClaimsOrbitScreen";\n\nexport function registerClaimsOrbitRoutes() {\n    return [\n        { path: \'/claims-orbit/incremental-109\', element: <ClaimsOrbitScreen mode="home" /> },\n        { path: \'/claims-orbit/incremental-109/details\', element: <ClaimsOrbitScreen mode="details" /> },\n        { path: \'/claims-orbit/incremental-109/history\', element: <ClaimsOrbitScreen mode="history" /> },\n    ];\n}\n', 'src/pages/ClaimsOrbitScreen.tsx': 'import React from "react";\nimport { ClaimsOrbitPanel } from "../components/ClaimsOrbitPanel";\nimport { ClaimsOrbitStats } from "../components/ClaimsOrbitStats";\n\nexport function ClaimsOrbitScreen(props: { mode: string }) {\n    return (\n        <section data-domain="claims_orbit" data-mode={props.mode}>\n            <ClaimsOrbitPanel heading="Claims Orbit" />\n            <ClaimsOrbitStats profile="incremental" channel="switch" />\n        </section>\n    );\n}\n', 'src/components/ClaimsOrbitPanel.tsx': 'import React from "react";\nexport function ClaimsOrbitPanel(props: { heading: string }) {\n    return <div className="panel-109">{props.heading} for claims_orbit</div>;\n}\n', 'src/components/ClaimsOrbitStats.tsx': 'import React from "react";\nexport function ClaimsOrbitStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 109</aside>;\n}\n', 'src/bridges/ClaimsOrbitBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "ClaimsOrbitBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "claims_orbit", "mode": "incremental", "boundary": "switch"},\n    {"name": "claims_orbit_detail", "mode": "detail", "boundary": "switch_detail"}\n  ],\n  "legacy_index": "registry_index.json"\n}\n', 'docs/claims_orbit_architecture.md': '# Claims Orbit architecture\nThe claims_orbit fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /claims-orbit/incremental-109\nBoundary hint: switch\nProfile: incremental\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/claims_orbit.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("claims_orbit", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/claims-orbit/incremental-109").toContain("claims");\n    });\n});\n', 'tools/enforce_claims_orbit_policy.py': "from __future__ import annotations\ndef verify_claims_orbit_policy() -> tuple[str, str]:\n    return ('incremental', 'switch')\n", 'scripts/rebuild_claims_orbit.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild claims_orbit in incremental mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/claims_orbit_seed.py': "SEED = {'domain': 'claims_orbit', 'profile': 'incremental', 'channel': 'switch', 'id': 109}\n", 'examples/claims_orbit_example.py': "EXAMPLE = {'path': '/claims-orbit/incremental-109', 'screen': 'ClaimsOrbitScreen', 'legacy_query_index': False}\n", 'reports/claims_orbit_run.md': 'run record for claims_orbit should be excluded by shared canon.\n', 'reports_real/claims_orbit_live.md': 'live report for claims_orbit should be excluded by shared canon.\n', 'generated/claims_orbit/snapshot.json': '{"domain": "claims_orbit", "generated": true, "profile": "incremental"}\n', 'runtime/claims_orbit/stream.log': 'claims_orbit runtime marker 109\n', '.shadow_incremental_switch_109.md': 'hidden note for claims_orbit incremental switch 109\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': False},
)
