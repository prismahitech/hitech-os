from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_104_fraud_lantern',
    description="Fraud Lantern synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('beacon', 'deterministic', 'fraud_lantern', 'homologated', 'observed_only', 'scanner'),
    source_files={'app/fraud_lantern/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.fraud_lantern.worker import build_fraud_lantern_packet\n\n@dataclass\nclass FraudLanternService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_fraud_lantern_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'fraud_lantern'\n        packet['profile'] = 'deterministic'\n        return packet\n\ndef load_fraud_lantern_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'beacon'\n    return data\n", 'app/fraud_lantern/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_fraud_lantern_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"fraud_lantern:{tenant}:104".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(104 / 3),\n        \'lane\': \'deterministic\',\n    }\n\nclass FraudLanternWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_fraud_lantern_packet(self.tenant)\n', 'app/fraud_lantern/routes.py': "from __future__ import annotations\nROUTES = [\n    '/fraud-lantern/deterministic-104',\n    '/fraud-lantern/deterministic-104/details',\n    '/fraud-lantern/deterministic-104/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { FraudLanternScreen } from "./pages/FraudLanternScreen";\nimport { registerFraudLanternRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/FraudLanternBridge";\n\nregisterFraudLanternRoutes();\nbridgeToDesktop({ lane: \'deterministic\', docket: \'beacon\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <FraudLanternScreen mode="deterministic" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { FraudLanternScreen } from "../pages/FraudLanternScreen";\n\nexport function registerFraudLanternRoutes() {\n    return [\n        { path: \'/fraud-lantern/deterministic-104\', element: <FraudLanternScreen mode="home" /> },\n        { path: \'/fraud-lantern/deterministic-104/details\', element: <FraudLanternScreen mode="details" /> },\n        { path: \'/fraud-lantern/deterministic-104/history\', element: <FraudLanternScreen mode="history" /> },\n    ];\n}\n', 'src/pages/FraudLanternScreen.tsx': 'import React from "react";\nimport { FraudLanternPanel } from "../components/FraudLanternPanel";\nimport { FraudLanternStats } from "../components/FraudLanternStats";\n\nexport function FraudLanternScreen(props: { mode: string }) {\n    return (\n        <section data-domain="fraud_lantern" data-mode={props.mode}>\n            <FraudLanternPanel heading="Fraud Lantern" />\n            <FraudLanternStats profile="deterministic" channel="beacon" />\n        </section>\n    );\n}\n', 'src/components/FraudLanternPanel.tsx': 'import React from "react";\nexport function FraudLanternPanel(props: { heading: string }) {\n    return <div className="panel-104">{props.heading} for fraud_lantern</div>;\n}\n', 'src/components/FraudLanternStats.tsx': 'import React from "react";\nexport function FraudLanternStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 104</aside>;\n}\n', 'src/bridges/FraudLanternBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "FraudLanternBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "fraud_lantern", "mode": "deterministic", "boundary": "beacon"},\n    {"name": "fraud_lantern_detail", "mode": "detail", "boundary": "beacon_detail"}\n  ],\n  "legacy_index": "query_index.json"\n}\n', 'docs/fraud_lantern_architecture.md': '# Fraud Lantern architecture\nThe fraud_lantern fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /fraud-lantern/deterministic-104\nBoundary hint: beacon\nProfile: deterministic\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/fraud_lantern.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("fraud_lantern", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/fraud-lantern/deterministic-104").toContain("fraud");\n    });\n});\n', 'tools/enforce_fraud_lantern_policy.py': "from __future__ import annotations\ndef verify_fraud_lantern_policy() -> tuple[str, str]:\n    return ('deterministic', 'beacon')\n", 'scripts/rebuild_fraud_lantern.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild fraud_lantern in deterministic mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/fraud_lantern_seed.py': "SEED = {'domain': 'fraud_lantern', 'profile': 'deterministic', 'channel': 'beacon', 'id': 104}\n", 'examples/fraud_lantern_example.py': "EXAMPLE = {'path': '/fraud-lantern/deterministic-104', 'screen': 'FraudLanternScreen', 'legacy_query_index': True}\n", 'reports/fraud_lantern_run.md': 'run record for fraud_lantern should be excluded by shared canon.\n', 'reports_real/fraud_lantern_live.md': 'live report for fraud_lantern should be excluded by shared canon.\n', 'generated/fraud_lantern/snapshot.json': '{"domain": "fraud_lantern", "generated": true, "profile": "deterministic"}\n', 'runtime/fraud_lantern/stream.log': 'fraud_lantern runtime marker 104\n', '.shadow_deterministic_beacon_104.md': 'hidden note for fraud_lantern deterministic beacon 104\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': True},
)
