from __future__ import annotations

from fixtures_py.base_case import ScannerFixtureCase

CASE = ScannerFixtureCase(
    name='case_073_session_courier',
    description="Session Courier synthetic scanner corpus with canonical, observed-only, excluded, and legacy naming edges.",
    tags=('curated', 'homologated', 'lantern', 'observed_only', 'scanner', 'session_courier'),
    source_files={'app/session_courier/service.py': "from __future__ import annotations\nimport json\nfrom dataclasses import dataclass\nfrom app.session_courier.worker import build_session_courier_packet\n\n@dataclass\nclass SessionCourierService:\n    tenant: str\n    region: str\n\n    def render_payload(self) -> dict[str, object]:\n        packet = build_session_courier_packet(self.tenant)\n        packet['region'] = self.region\n        packet['domain'] = 'session_courier'\n        packet['profile'] = 'curated'\n        return packet\n\ndef load_session_courier_snapshot(raw: str) -> dict[str, object]:\n    data = json.loads(raw)\n    data['source'] = 'lantern'\n    return data\n", 'app/session_courier/worker.py': 'from __future__ import annotations\nimport hashlib\nimport math\n\ndef build_session_courier_packet(tenant: str) -> dict[str, object]:\n    token = hashlib.sha1(f"session_courier:{tenant}:73".encode(\'utf-8\')).hexdigest()[:12]\n    return {\n        \'tenant\': tenant,\n        \'checksum\': token,\n        \'stability_band\': math.floor(73 / 3),\n        \'lane\': \'curated\',\n    }\n\nclass SessionCourierWorker:\n    def __init__(self, tenant: str) -> None:\n        self.tenant = tenant\n\n    def execute(self) -> dict[str, object]:\n        return build_session_courier_packet(self.tenant)\n', 'app/session_courier/routes.py': "from __future__ import annotations\nROUTES = [\n    '/session-courier/curated-073',\n    '/session-courier/curated-073/details',\n    '/session-courier/curated-073/history',\n]\n", 'src/main.tsx': 'import React from "react";\nimport { createRoot } from "react-dom/client";\nimport { BrowserRouter } from "react-router-dom";\nimport { SessionCourierScreen } from "./pages/SessionCourierScreen";\nimport { registerSessionCourierRoutes } from "./routes/register";\nimport { bridgeToDesktop } from "./bridges/SessionCourierBridge";\n\nregisterSessionCourierRoutes();\nbridgeToDesktop({ lane: \'curated\', docket: \'lantern\' });\ncreateRoot(document.getElementById("root")!).render(\n    <BrowserRouter>\n        <SessionCourierScreen mode="curated" />\n    </BrowserRouter>\n);\n', 'src/routes/register.ts': 'import { SessionCourierScreen } from "../pages/SessionCourierScreen";\n\nexport function registerSessionCourierRoutes() {\n    return [\n        { path: \'/session-courier/curated-073\', element: <SessionCourierScreen mode="home" /> },\n        { path: \'/session-courier/curated-073/details\', element: <SessionCourierScreen mode="details" /> },\n        { path: \'/session-courier/curated-073/history\', element: <SessionCourierScreen mode="history" /> },\n    ];\n}\n', 'src/pages/SessionCourierScreen.tsx': 'import React from "react";\nimport { SessionCourierPanel } from "../components/SessionCourierPanel";\nimport { SessionCourierStats } from "../components/SessionCourierStats";\n\nexport function SessionCourierScreen(props: { mode: string }) {\n    return (\n        <section data-domain="session_courier" data-mode={props.mode}>\n            <SessionCourierPanel heading="Session Courier" />\n            <SessionCourierStats profile="curated" channel="lantern" />\n        </section>\n    );\n}\n', 'src/components/SessionCourierPanel.tsx': 'import React from "react";\nexport function SessionCourierPanel(props: { heading: string }) {\n    return <div className="panel-073">{props.heading} for session_courier</div>;\n}\n', 'src/components/SessionCourierStats.tsx': 'import React from "react";\nexport function SessionCourierStats(props: { profile: string; channel: string }) {\n    return <aside data-profile={props.profile} data-channel={props.channel}>scanner fixture 073</aside>;\n}\n', 'src/bridges/SessionCourierBridge.ts': 'export function bridgeToDesktop(payload: Record<string, unknown>) {\n    const channel = (window as any).QWebChannel ?? "qwebchannel";\n    return { channel, payload, bridgeName: "SessionCourierBridge", transport: "desktop_bridge" };\n}\n', 'public/modules.config.json': '{\n  "modules": [\n    {"name": "session_courier", "mode": "curated", "boundary": "lantern"},\n    {"name": "session_courier_detail", "mode": "detail", "boundary": "lantern_detail"}\n  ],\n  "legacy_index": "registry_index.json"\n}\n', 'docs/session_courier_architecture.md': '# Session Courier architecture\nThe session_courier fixture is an observed-only synthetic tree for scanner validation.\nIt mixes canonical runtime surfaces with observed-only docs, tools, and tests.\nRoute root: /session-courier/curated-073\nBoundary hint: lantern\nProfile: curated\nThis note exists so the scanner can prove docs are read-adjacent but non-canonical.\n', 'tests/session_courier.spec.ts': 'import { describe, it, expect } from "vitest";\ndescribe("session_courier", () => {\n    it("keeps scanner corpora noisy but explainable", () => {\n        expect("/session-courier/curated-073").toContain("session");\n    });\n});\n', 'tools/enforce_session_courier_policy.py': "from __future__ import annotations\ndef verify_session_courier_policy() -> tuple[str, str]:\n    return ('curated', 'lantern')\n", 'scripts/rebuild_session_courier.py': 'from __future__ import annotations\ndef main() -> str:\n    return "rebuild session_courier in curated mode"\nif __name__ == "__main__":\n    print(main())\n', 'fixtures/session_courier_seed.py': "SEED = {'domain': 'session_courier', 'profile': 'curated', 'channel': 'lantern', 'id': 73}\n", 'examples/session_courier_example.py': "EXAMPLE = {'path': '/session-courier/curated-073', 'screen': 'SessionCourierScreen', 'legacy_query_index': False}\n", 'reports/session_courier_run.md': 'run record for session_courier should be excluded by shared canon.\n', 'reports_real/session_courier_live.md': 'live report for session_courier should be excluded by shared canon.\n', 'generated/session_courier/snapshot.json': '{"domain": "session_courier", "generated": true, "profile": "curated"}\n', 'runtime/session_courier/stream.log': 'session_courier runtime marker 073\n', '.shadow_curated_lantern_073.md': 'hidden note for session_courier curated lantern 073\n'},
    expected_summary={'canonical_module_min': 9, 'observe_only_min': 7, 'excluded_min': 4, 'route_count_min': 3, 'boundary_count_min': 4, 'legacy_query_index_seen': False},
)
